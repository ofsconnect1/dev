1. We are using bcrypt function which is the default password hash algorithm.

2. for login and logout functionality, we are using flask extension 'flask-login' -- it makes it very easy to manage user sessions.
   pip install flask-login

3. Used Pillow package/library for image handling (resizing image)
4. 
